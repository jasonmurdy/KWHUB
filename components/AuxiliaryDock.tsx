import React from 'react';
// This component is now deprecated as utilities have moved to the Header.
const AuxiliaryDock: React.FC<{ activeTab: string, onTabChange: (tab: string) => void }> = () => null;
export default AuxiliaryDock;